<% from_for @todo, :url => todos_path do|f| %>
	
	<%= f.label :title, "Title" %>
	<%= f.text_field :title %>

	<%= f.label :date, "Date" %>
	<%= f.text_field :Date %>

	<%= f.label :description, "Description" %>
	<%= f.text_area :Description %>

	<%= f.submit "Create" %>

<% end %>